/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental:{
        serverActions:true
    },
    images: {
        domains: ['purecatamphetamine.github.io'],
    },
}

module.exports = nextConfig
